import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.alphalog.app',
  appName: 'AlphaLog',
  webDir: 'dist',
  android: {
    backgroundColor: '#0D0E12',
  },
}

export default config
